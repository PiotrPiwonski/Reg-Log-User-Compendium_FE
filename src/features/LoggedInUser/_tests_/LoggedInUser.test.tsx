import { render } from '../../../utils/customRender';
import { LoggedInUser } from '../LoggedInUser';

describe('LoggedInUser test', () => {
  it('should render LoggedInUser', () => {
    render(<LoggedInUser />);
  });
});
